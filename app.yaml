# App Engine configuration file for the bulletin board application.
# Uses 3.11 runtime because some stuff don't work in 3.12
runtime: python311

env_variables:
  GOOGLE_APPLICATION_CREDENTIALS: drawing-sa-key.json

handlers:
  - url: /static
    static_dir: static
  - url: /.*
    script: auto
